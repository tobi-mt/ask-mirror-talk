# Railway deployment configuration
# This file is automatically detected by Railway

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Don't specify startCommand - let Dockerfile CMD handle it
# Railway will use the CMD from Dockerfile which properly expands $PORT
healthcheckPath = "/health"
healthcheckTimeout = 600
