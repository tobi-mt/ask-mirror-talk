# Railway deployment configuration
# This file is automatically detected by Railway

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Don't specify startCommand or healthcheck here
# Configure these per-service in Railway Dashboard:
# - mirror-talk-api: Use default CMD, enable healthcheck to /health
# - mirror-talk-ingestion: Custom start command, disable healthcheck
